import React from 'react'

const AddValue = ({incrementCount}) => {
    return (
    <div>
        <button onClick={incrementCount}>Click to increase by 1</button>
    </div>

    )

}



export default AddValue;